[build-system]
requires = ["setuptools >= 61.0]
build-backend = "setuptools.build_meta"

[project]
name = "revpy"
version = "0.1.0"
description = "Revenue Management Library"

dependencies = [
    "numpy",
    "scipy",
    "matplotlib"
]